from django.urls import path
from django.views.generic import TemplateView
from . import views

urlpatterns = [
    # path('<profile_id:int>/', views.profile, name='profile'),
    # path('<profile_id:int>/some_op', views.profile_ops, name='profile-ops'),
]
